---
export const prerender = false;

// Legacy post route — disabled.
// All posts are now served under /w/:workspaceSlug/posts/:slug.
// This file is intentionally kept (rather than deleted) so that any incoming
// requests to the old /posts/:slug URL pattern are gracefully redirected to
// /404 instead of producing a hard 404 at the Astro router level.
return Astro.redirect("/404");
---
